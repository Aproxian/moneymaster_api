/** Ledger categories used only for cross-account and wallet↔wallet transfers. */
const TRANSFER_SEND = "TRANSFER_SEND";
const TRANSFER_RECEIVE = "TRANSFER_RECEIVE";

module.exports = { TRANSFER_SEND, TRANSFER_RECEIVE };
